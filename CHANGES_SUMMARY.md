# 位置感知模型改造总结

## 改造目标

将原有的按怪物ID聚合的预测系统改造为考虑位置顺序的系统，使模型能够理解怪物的出场顺序对战斗结果的影响。

## 核心变化

### 1. 数据格式变化

**旧格式（按ID聚合）：**
```
[怪物1数量, 怪物2数量, ..., 怪物77数量, 场地特征, ..., Result]
总长度: 77 + 6 + 77 + 6 + 1 = 167列
```

**新格式（位置顺序）：**
```
[左1_ID, 左1_数量, 左2_ID, 左2_数量, 左3_ID, 左3_数量,
 右1_ID, 右1_数量, 右2_ID, 右2_数量, 右3_ID, 右3_数量,
 场地特征L(6), 场地特征R(6), Result]
总长度: 12 + 6 + 6 + 1 = 25列
```

### 2. 模型架构变化

**旧模型（UnitAwareTransformer）：**
- 输入：按ID聚合的数量向量
- 处理：TopK选择 → 嵌入 → Transformer
- 特点：不考虑位置，只关心种类和数量

**新模型（PositionAwareTransformer）：**
- 输入：6个位置的(ID, 数量)对
- 处理：ID嵌入 + 位置嵌入 + 数量融合 → Transformer
- 特点：考虑出场顺序，理解时机

## 新增文件

1. **constants.py** - 位置相关常量定义
   - `POSITIONS_PER_SIDE = 3`
   - `FEATURES_PER_POSITION = 2`

2. **train_position.py** - 位置感知模型训练脚本
   - `ArknightsPositionDataset` - 位置数据集类
   - `PositionAwareTransformer` - 位置感知模型
   - 完整的训练流程

3. **predict_position.py** - 位置感知预测模块
   - `CannotModelPosition` - 位置模型加载和预测
   - `get_prediction_with_position()` - 位置数据预测接口

4. **tools/convert_to_position_format.py** - 数据格式转换工具
   - 将旧格式数据转换为新格式
   - 注意：转换是近似的，真实数据需重新收集

5. **POSITION_MODEL_README.md** - 详细使用文档

6. **test_position_model.py** - 测试脚本

7. **CHANGES_SUMMARY.md** - 本文档

## 修改文件

### main.py
- 修改模型加载逻辑，优先使用位置模型
- 修改 `get_prediction()` 方法，支持两种模型
- 兼容旧模型和新模型

### auto_fetch.py
- 修改 `fill_data()` 方法
- 同时保存两种格式的数据：
  - `arknights.csv` - 旧格式（兼容）
  - `arknights_position.csv` - 新格式

### input_panel_ui.py
- 新增 `get_position_data()` 方法
- 返回位置顺序的数据结构

## 使用流程

### 阶段1：数据收集
```bash
# 运行主程序，自动收集两种格式的数据
python main.py
```

### 阶段2：训练模型
```bash
# 训练位置感知模型
python train_position.py
```

### 阶段3：使用模型
```bash
# 程序自动检测并使用位置模型
python main.py
```

### 可选：转换旧数据
```bash
# 将旧数据转换为新格式（近似）
python tools/convert_to_position_format.py arknights.csv
```

## 兼容性

### 向后兼容
- 保留了旧格式数据收集
- 保留了旧模型支持
- 自动检测并选择可用模型

### 模型选择优先级
1. 位置感知模型（`best_model_position_*.pth`）
2. 传统PyTorch模型（`best_model_*.pth`）
3. ONNX模型（`best_model_*.onnx`）

## 关键设计决策

### 1. 为什么是3个位置？
- 游戏中每侧最多显示3个怪物单位
- 对应识别系统的6个区域（region_id 0-5）

### 2. 为什么保留旧格式？
- 向后兼容
- 对比实验
- 数据备份

### 3. 转换策略
- 按数量降序排列（数量多的优先）
- 可以根据实际情况调整排序规则

### 4. 空位置处理
- 记录为 (0, 0)
- 模型使用mask机制自动忽略

## 预期效果

### 理论优势
- 捕捉出场顺序的影响
- 理解先手优势
- 更准确的时机预测

### 实际效果取决于
- 位置信息是否真的影响胜率
- 数据质量（位置是否准确）
- 数据量（是否足够训练）

## 测试验证

运行测试脚本：
```bash
python test_position_model.py
```

测试内容：
1. 位置数据格式正确性
2. 预测输入格式正确性
3. 数据转换逻辑正确性
4. 模块导入兼容性

## 注意事项

### 1. 数据收集
- 新数据会同时保存两种格式
- 确保识别准确，位置信息才有意义

### 2. 模型训练
- 位置模型参数更多，需要更多数据
- 建议至少10000+样本

### 3. 性能对比
- 训练后对比新旧模型准确率
- 根据结果决定是否全面切换

### 4. 数据转换
- 转换的数据只是近似
- 真实效果需要重新收集的数据

## 下一步建议

1. **短期（测试阶段）**
   - 收集1000-5000条位置数据
   - 训练初始模型
   - 对比准确率

2. **中期（验证阶段）**
   - 收集10000+条数据
   - 优化模型参数
   - A/B测试对比

3. **长期（生产阶段）**
   - 根据效果决定是否切换
   - 持续收集数据优化
   - 考虑导出ONNX加速

## 回滚方案

如果新模型效果不佳：
1. 删除 `models/` 下所有 `*position*` 文件
2. 程序自动回退到旧模型
3. 旧格式数据仍在收集，不影响训练

## 技术债务

1. `input_panel_ui.py` 的 `get_position_data()` 方法目前是简单实现
   - 按ID排序取前3个
   - 实际应该保持识别时的位置顺序

2. 数据转换工具是近似的
   - 无法恢复真实位置信息
   - 只能用于初步测试

3. 需要更新UI来显示位置信息
   - 当前UI按ID排序显示
   - 应该按位置顺序显示

## 总结

这次改造实现了从"按ID聚合"到"按位置顺序"的完整转变，同时保持了向后兼容性。系统现在可以：

✅ 同时收集两种格式的数据
✅ 训练位置感知模型
✅ 自动选择最佳模型
✅ 兼容旧模型和旧数据
✅ 提供完整的测试和文档

核心改进是让模型能够理解"谁先出场"这个关键信息，而不仅仅是"有哪些怪物"。
