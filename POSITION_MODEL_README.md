# 位置感知模型使用指南

## 概述

新的位置感知模型考虑了怪物的出场位置顺序，而不是简单地按怪物ID聚合。这使得模型能够更准确地预测战斗结果，因为出场顺序会影响战斗时机。

## 数据格式变化

### 旧格式（按ID聚合）
```
[1L, 2L, 3L, ..., 77L, 场地L, 1R, 2R, ..., 77R, 场地R, Result]
```
- 每个数字代表该ID怪物的数量
- 丢失了位置信息

### 新格式（位置顺序）
```
[左1_ID, 左1_数量, 左2_ID, 左2_数量, 左3_ID, 左3_数量,
 右1_ID, 右1_数量, 右2_ID, 右2_数量, 右3_ID, 右3_数量,
 场地L, 场地R, Result]
```
- 保留了6个位置的信息（左3个，右3个）
- 每个位置记录 (怪物ID, 数量)
- 空位置记录为 (0, 0)

## 使用步骤

### 1. 收集新数据

运行主程序时，会自动同时保存两种格式的数据：
- `arknights.csv` - 旧格式（兼容）
- `arknights_position.csv` - 新格式（位置顺序）

```bash
python main.py
```

### 2. 转换旧数据（可选）

如果你有旧格式的数据想要转换：

```bash
python tools/convert_to_position_format.py arknights.csv -o arknights_position.csv
```

**注意**：转换只是近似的，因为旧数据丢失了真实的位置信息。转换脚本会按照数量降序排列怪物。真正准确的数据需要重新收集。

### 3. 训练位置感知模型

```bash
python train_position.py
```

配置参数在 `train_position.py` 的 `main()` 函数中：
- `data_file`: 数据文件路径（默认 `arknights_position.csv`）
- `batch_size`: 批次大小（默认 1024）
- `embed_dim`: 嵌入维度（默认 128）
- `n_layers`: Transformer层数（默认 3）
- `epochs`: 训练轮数（默认 200）

训练完成后，模型会保存到 `models/` 目录：
- `best_model_position_acc_*.pth` - 最佳准确率模型
- `best_model_position_loss_*.pth` - 最佳损失模型
- `best_model_position_full_*.pth` - 最后一轮模型

### 4. 使用模型预测

程序会自动检测并使用位置感知模型（如果存在）：

```python
# 在 main.py 中会自动选择
# 优先级：位置感知模型 > 传统PyTorch模型 > ONNX模型
```

### 5. 导出ONNX模型（可选）

```python
from predict_position import CannotModelPosition

model = CannotModelPosition("models")
model.export_onnx("models/best_model_position_full.onnx")
```

## 模型架构

### PositionAwareTransformer

新模型的特点：
1. **怪物ID嵌入**：每个怪物有独特的嵌入向量
2. **位置嵌入**：6个位置（左1-3，右1-3）各有位置编码
3. **数量融合**：将数量信息融入嵌入向量
4. **场地特征**：作为额外的token加入序列
5. **双向注意力**：
   - 敌方注意力：左右互相关注
   - 友方注意力：同侧内部关注

### 与旧模型的区别

| 特性 | 旧模型 | 新模型 |
|------|--------|--------|
| 输入方式 | 按ID聚合，TopK选择 | 按位置顺序，固定3个位置 |
| 位置信息 | 无 | 有位置嵌入 |
| 序列长度 | 可变（TopK） | 固定（3+1场地） |
| 适用场景 | 只关心怪物种类和数量 | 关心出场顺序和时机 |

## 数据收集建议

为了获得最佳效果，建议：

1. **重新收集数据**：使用新版本程序收集数据，确保位置信息准确
2. **标注位置**：如果手动标注，确保记录真实的出场位置
3. **数据量**：位置模型参数更多，建议至少10000+样本
4. **平衡性**：确保各种位置组合都有足够样本

## 识别结果到位置数据的映射

识别系统返回6个区域的结果：
- `region_id` 0, 1, 2 → 左侧位置 1, 2, 3
- `region_id` 3, 4, 5 → 右侧位置 1, 2, 3

每个区域包含：
- `matched_id`: 怪物ID（1-77）
- `number`: 数量
- `confidence`: 识别置信度

## 常见问题

### Q: 旧模型和新模型可以共存吗？
A: 可以。程序会自动检测并优先使用位置模型。你可以保留两种模型文件。

### Q: 如何切换回旧模型？
A: 删除或重命名 `models/` 目录下所有包含 "position" 的模型文件。

### Q: 转换的数据能用吗？
A: 可以用于初步测试，但效果会打折扣。建议重新收集真实的位置数据。

### Q: 位置顺序是怎么定义的？
A: 从左到右，从上到下。具体对应游戏中怪物的出场顺序。

### Q: 如果某个位置没有怪物怎么办？
A: 记录为 (0, 0)，模型会自动mask掉这个位置。

## 性能对比

理论上，位置感知模型应该能够：
- 更准确地预测需要时机配合的战斗
- 更好地理解先手优势
- 捕捉位置相关的战术组合

实际效果取决于：
- 数据质量（位置信息是否准确）
- 数据量（是否足够训练）
- 游戏机制（位置是否真的重要）

建议收集足够数据后对比两种模型的准确率。

## 文件清单

新增文件：
- `constants.py` - 位置相关常量定义
- `train_position.py` - 位置感知模型训练脚本
- `predict_position.py` - 位置感知模型预测模块
- `tools/convert_to_position_format.py` - 数据格式转换工具
- `POSITION_MODEL_README.md` - 本文档

修改文件：
- `main.py` - 添加位置模型支持
- `auto_fetch.py` - 同时保存两种格式数据
- `input_panel_ui.py` - 添加位置数据获取方法

## 下一步

1. 收集至少1000条位置数据
2. 训练初始模型
3. 对比新旧模型准确率
4. 根据结果决定是否全面切换
5. 持续收集数据优化模型
